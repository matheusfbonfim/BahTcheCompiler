BARBARIDADE GURI sum(GURI a, GURI b)
{
    GURI sum;
    sum = a + b;
	LARGUEIMAO sum;
}

BAHTCHE{
    GURI a, GURI b, GURI result;
    INPUTCHE(a);
    INPUTCHE(b);

    result = BARBARIDADE uberfuncao(a,b);
    PRINTCHE(result);

}