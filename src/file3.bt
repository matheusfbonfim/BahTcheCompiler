BARBARIDADE GURI sum(GURI a, GURI b)
{
    GURI sum;
    sum = a + b;
	LARGUEIMAO sum;
}

BAHTCHE{
    GURI a, GURI b, GURI result, FANDANGO e4, FANDANGO e5, FANDANGO e6;
    INPUTCHE(a);
    INPUTCHE(b);

    result = BARBARIDADE sum(a,b);
    PRINTCHE(result);

}