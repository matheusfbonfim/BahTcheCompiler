BARBARIDADE GURI sum(GURI a, GURI b)
{
    GURI sum;
    sum = a + b + 0;
	LARGUEIMAO sum;
}

BAHTCHE{
    GURI a, GURI b, GURI result;
    INPUTCHE(a);
    INPUTCHE(b);

    result = BARBARIDADE sum(a,b, b);
    PRINTCHE(result);

}